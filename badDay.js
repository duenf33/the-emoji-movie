const badDay = require('./day');


const arr = [];
const func = function(){
    // console.log(arr);
    // for(let i = 0; i < str.length; i++){
        // arr.push(i)
        console.log(`this is a ${badDay}, for everyone`);
    }
    // return arr;
// }
// func(badDay);
// for(const badDay of badDays){
    setInterval(func, 1000)
// }                       